/**
 * Mapa de Riesgos Residual Page
 * Muestra solo el mapa de riesgo residual
 */

import MapaPage from './MapaPage';

export default function MapaResidualPage() {
  // Esta página simplemente redirige a MapaPage con tipo residual
  // En una implementación más completa, podríamos pasar props a MapaPage
  return <MapaPage />;
}

